<UserControl x:Class="HalconWpf.Views.HalconView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:halcon="clr-namespace:HalconDotNet;assembly=halcondotnet"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             MouseLeave="UserControl_MouseLeave">
    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#FFF2F2F2" Padding="6,4"
                BorderBrush="#FFD8D8D8" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <Button Content="适应窗口" Command="{Binding FitWindowCommand}" ToolTip="图像适应窗口大小"
                        Padding="10,4"/>
            </StackPanel>
        </Border>
        <Border BorderBrush="#FFBDBDBD" BorderThickness="1" Margin="0">
            <halcon:HSmartWindowControlWPF x:Name="HalconWindow"
                                           HMoveContent="True"
                                           HZoomContent="WheelForwardZoomsIn"
                                           HKeepAspectRatio="True"
                                           HDoubleClickToFitContent="True"
                                           HMouseMove="HalconWindow_HMouseMove"/>
        </Border>
    </DockPanel>
</UserControl>
