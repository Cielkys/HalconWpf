# 2026-09-22

- 搭建 HalconWpf 主界面：顶部工具栏 + 底部状态栏 + 中间（左输出参数面板 `OutputParamsView`、右 `HalconView` 内含工具栏与 `HSmartWindowControlWPF`），支持打开图片显示、缩放/适应/1:1、鼠标 row/col 状态栏显示、image_width/image_height 参数输出（为后续 area[]/row[]/col[] 预留 `SetParameters` API）。
- 用临时探针（net48 + net8 双目标）验证 halcondotnet 17.12 兼容性，结论：net8.0-windows 可用，XAML 引用控件需补 `System.Drawing.Common` 包；探针已删除。
- 修正 `App.xaml` StartupUri 指向 `Views/MainWindow.xaml`；`dotnet build` 0 警告 0 错误，冒烟启动通过。
- 按用户要求调整 UI：删除顶部"关于"按钮（含 `About_Click`）、删除 HalconView 的"放大/缩小"按钮（含 `ZoomIn/ZoomOut/Zoom` 方法），两处工具栏均弃用 `ToolBarTray`/`ToolBar`，改为 `Border + StackPanel + Button`；重新 build 0 警告 0 错误（期间需先关闭正在运行的实例以解除 exe 占用）。
- 状态栏不再显示图片完整路径（`OnImageOpened` 不再设置 `TxtStatus.Text = path`），仍保留 `TxtImageInfo` 的文件名 + 分辨率。
- 用户反馈**"写的太复杂了，我看不懂了"**，再次大幅简化：3 个 ViewModel 合并为单个 `MainViewModel`（含 Rows、ImageInfo、MousePositionText、StatusText、CurrentImage、HasImage、OpenImage/ClearOutput 命令、AddParameters、ImageChanged 事件）；删除 HalconViewModel、OutputParamsViewModel、IFileDialogService、FileDialogService、OutputParameter。HalconView 改为继承主窗口 DataContext，"适应窗口/1:1" 改用 Click 处理器 + `IsEnabled="{Binding HasImage}"`；状态栏绑定去掉 `Halcon.` 前缀；OutputParamsView 去掉子 DataContext，直接绑 `Rows`/`ClearOutputCommand`。build 0 警告 0 错误。
- 用户随后发指令**"恢复"**：简化版全部回滚，10 个文件原样写回（MainViewModel/HalconViewModel/OutputParamsViewModel、IFileDialogService/FileDialogService、OutputParameter、HalconView.xaml(.cs)、MainWindow.xaml、OutputParamsView.xaml），恢复 3 ViewModel + 服务层分层架构。C# 编译无错误，但 exe 被运行中的 HalconWpf(PID 25076) 占用导致输出复制失败（MSB3027），需关掉运行实例后重新 build。**结论：分层版才是用户要的最终形态，勿再主动简化。**
- 按用户要求删了两个按钮：顶部工具栏"清空输出"（MainWindow.xaml）、HalconView 工具栏"1:1"（HalconView.xaml）。VM 侧 `ClearOutputCommand`/`ActualSizeCommand`/`OnActualSizeRequested` 等代码保留未删（仅去掉 UI 入口）。build 0 警告 0 错误。
- 用户要求"把不用的功能从业务逻辑里删除"：清掉无 UI 入口的死代码——`HalconViewModel` 删 `ActualSize` 命令、`ActualSizeRequested` 事件、`ImagePath`/`_imagePath`；`MainViewModel` 删 `ClearOutput` 命令（并去掉 `using CommunityToolkit.Mvvm.Input`、`partial`）；`HalconView.xaml.cs` 删 `OnActualSizeRequested` 及订阅/退订。保留：`OutputParamsViewModel.ClearCommand`（面板"清空"按钮仍绑定）。C# 编译无错误，但 exe 被运行实例 HalconWpf(PID 19748) 占用，输出复制失败需关程序后重 build。
- 用户要求"输出参数不要索引"：`OutputParamsView.xaml` 删"索引"列，`ParameterRow` 去掉 `Index`，`OutputParamsViewModel.SetParameters` 改为一个参数一行、数组值用 ", " 拼接（`string.Join` + `Select`，`IReadOnlyList<double>` 无 ConvertAll）。表格现在只有 参数/值 两列。build 0 警告 0 错误。
- 用户指出有框架不该手写 `get => _x; set => SetProperty(...)` 属性：`HalconViewModel.ImageInfo`/`MousePositionText`、`MainViewModel.StatusText` 全部改为 `[ObservableProperty]` 写在字段上，删手写包装；`MainViewModel` 类声明恢复 `partial`。注意 [ObservableProperty] 生成的 setter 是 public（原 ImageInfo/MousePositionText 为 private set）。build 0 警告 0 错误。
- MVVM 重构完成后，按用户要求改为使用已有框架 `CommunityToolkit.Mvvm`（csproj 早已引用 8.4.2）：删除自写的 `Infrastructure/ObservableObject.cs` 与 `RelayCommand.cs`，三个 ViewModel 改为 `partial class` 继承框架 `ObservableObject`，命令改用 `[RelayCommand]` 源生成（OpenImage/FitWindow/ActualSize/ClearOutput/Clear，Fit 与 ActualSize 用 `CanExecute = nameof(CanAdjustDisplay)`，加载图片后调用 `NotifyCanExecuteChanged()`）。补充 `MainViewModel` 的 `using HalconDotNet;`、`HalconView.xaml.cs` 的 `using System.Windows.Input;`；build 0 警告 0 错误。
- Fsm 项目（空白 net8.0-windows WPF）：用户在做"虚拟串口仿真扫码枪"学习计划，要求**教他怎么写**（教学而非直接实现）。已给出 6 步教学：0) 定帧格式 `AA 55|LEN|ASCII条码|CRC16(低字节在前)|0D 0A`；1) Crc16.Modbus（初值0xFFFF/多项式0xA001，需在线工具交叉验证）；2) 扮演扫码枪往 COM4 发帧；3) COM3 DataReceived 接收 + 字节↔hex 互转；4) 解析验 CRC + 篡改字节实验；5) FSM 五状态（等待PLC信号/触发扫码/解析条码/复位/报警）用 switch 元组实现，含超时（DispatcherTimer）与报警复位分支；6) PLC 信号先用按钮顶替，进阶用 Modbus（**本程序=主站，Modbus Slave=从站扮演PLC**，Modbus Poll 仅用来验证从站；第二对虚拟串口 COM5↔COM6，功能码03 读寄存器，值1触发）。需 NuGet `System.IO.Ports`。尚未落码，等用户要求再写入项目。
- **用户反馈："你要教我怎么写啊，直接让我对着代码写我能学到什么"** → 教学方式改为：分课讲原理 + 练习题（验收标准/可查 API/禁止抄的部分）+ 用户写完贴回批改，不给完整成品代码。已发布第 1 课（串口收发+hex：打开 COM3/COM4、发 0x55、DataReceived+Dispatcher.BeginInvoke 更新日志、自测 3 问），等待用户提交代码。教学偏好已写入 MEMORY.md。
- 用户追问"一点提示也不给的吗" → 提示粒度定为**骨架级**：可给控件清单、字段清单、留空伪代码（`___` 填空）、易错点、报错对照表、自测答题方向；仍不给可直接运行的完整代码。已发布提示卡 1.1（含考点：为何只需/需两个串口的理由、ToString("X2") 含义、Read 不保证读全）。
